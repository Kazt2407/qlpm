<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= content_for?(:title) ? "#{yield(:title)} | " : "" %>QLPM</title>
  <link rel="icon" type="image/png" href="/icon.png">
  <link rel="apple-touch-icon" href="/icon.png">

  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <style>
    * { font-family: 'Be Vietnam Pro', sans-serif; }
    body { background: linear-gradient(180deg, #f8fafc 0%, #eef6ff 100%); }
    .shell-card { background: rgba(255,255,255,.82); backdrop-filter: blur(14px); box-shadow: 0 10px 35px rgba(15, 23, 42, .08); }
    [class*="rounded"] { border-radius: 8px !important; }
    .border, .border-r, .border-l, .border-t, .border-b { border-width: 3px !important; }
  </style>
</head>
<body class="min-h-screen text-slate-900">
  <div class="flex min-h-screen">
    <aside class="sticky top-0 flex h-screen w-72 flex-col border-r border-slate-200/80 bg-white/90 px-5 py-6">
      <div class="mb-8">
        <div class="flex items-center gap-3">
          <img src="/logo.png" alt="QLPM" class="h-12 w-12 rounded-2xl object-contain">
          <div>
            <p class="text-sm font-semibold text-slate-900">Quản lý phòng máy</p>
            <p class="text-xs text-slate-500">QLPM</p>
          </div>
        </div>
      </div>

      <div class="min-h-0 flex-1 overflow-y-auto pb-4">
        <nav class="space-y-2">
          <% if can_manage_system? %>
            <%= link_to root_path, class: nav_link_classes("dashboard") do %>
              <span>Bảng điều khiển</span>
            <% end %>

            <%= link_to assets_path, class: nav_link_classes("assets") do %>
              <span>Đối tượng sử dụng</span>
            <% end %>

            <%= link_to rooms_path, class: nav_link_classes("rooms") do %>
              <span>Phòng máy</span>
            <% end %>

            <%= link_to borrows_path, class: nav_link_classes("borrows") do %>
              <span>Lịch mượn và trả</span>
            <% end %>

            <%= link_to schedule_path, class: nav_link_classes("schedules") do %>
              <span>Lịch sử dụng</span>
            <% end %>

            <%= link_to veyon_hosts_path, class: nav_link_classes("hosts") do %>
              <span>Điều khiển phòng máy</span>
            <% end %>

            <%= link_to work_orders_path, class: nav_link_classes("work_orders") do %>
              <span>Bảo trì</span>
            <% end %>

            <%= link_to reports_path, class: nav_link_classes("reports") do %>
              <span>Báo cáo</span>
            <% end %>

            <%= link_to users_path, class: nav_link_classes("users") do %>
              <span>Người dùng</span>
            <% end %>
          <% elsif can_review_borrows? %>
            <%= link_to borrows_path, class: nav_link_classes("borrows") do %>
              <span>Duyệt phiếu mượn</span>
            <% end %>
            <%= link_to schedule_path, class: nav_link_classes("schedules") do %>
              <span>Lịch sử dụng</span>
            <% end %>
            <%= link_to veyon_hosts_path, class: nav_link_classes("hosts") do %>
              <span>Điều khiển phòng máy</span>
            <% end %>
          <% else %>
            <%= link_to borrows_path, class: nav_link_classes("borrows") do %>
              <span>Phiếu mượn của tôi</span>
            <% end %>
            <%= link_to new_work_order_path, class: nav_link_classes("work_orders") do %>
              <span>Báo lỗi thiết bị</span>
            <% end %>
          <% end %>
        </nav>

        <div class="shell-card mt-8 rounded-3xl border border-slate-200/70 p-4">
          <p class="text-xs font-semibold uppercase tracking-[0.2em] text-slate-500">Tổng quan nhanh</p>
          <div class="mt-4 space-y-3 text-sm">
            <% if can_manage_system? %>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Đối tượng</span>
                <span class="font-semibold text-slate-900"><%= @sidebar_assets %></span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Phòng</span>
                <span class="font-semibold text-slate-900"><%= @sidebar_rooms %></span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Sẵn sàng</span>
                <span class="font-semibold text-emerald-700"><%= @sidebar_active %></span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Đang mở</span>
                <span class="font-semibold text-amber-700"><%= @sidebar_open_borrows %></span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Bảo trì mở</span>
                <span class="font-semibold text-red-700"><%= @sidebar_open_work_orders || 0 %></span>
              </div>
            <% elsif can_review_borrows? %>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Chờ duyệt</span>
                <span class="font-semibold text-sky-700"><%= @sidebar_active %></span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Đang mở</span>
                <span class="font-semibold text-amber-700"><%= @sidebar_open_borrows %></span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Bảo trì mở</span>
                <span class="font-semibold text-red-700"><%= @sidebar_open_work_orders || 0 %></span>
              </div>
            <% else %>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Phiếu của tôi</span>
                <span class="font-semibold text-slate-900"><%= @sidebar_assets %></span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Chờ duyệt</span>
                <span class="font-semibold text-sky-700"><%= @sidebar_active %></span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-slate-500">Đang mở</span>
                <span class="font-semibold text-amber-700"><%= @sidebar_open_borrows %></span>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <div class="shrink-0 pt-4">
        <% if current_user %>
          <div class="mb-3">
            <p class="text-sm font-semibold text-slate-900"><%= current_user.full_name %></p>
            <p class="text-xs text-slate-500"><%= requester_user? ? current_user.user_type_label : current_user.role_label %></p>
          </div>
          <%= button_to "Đăng xuất", logout_path, method: :delete, class: "w-full rounded-xl bg-slate-100 px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-200" %>
        <% else %>
          <p class="text-xs text-slate-500">Chưa đăng nhập</p>
        <% end %>
      </div>
    </aside>

    <main class="flex-1 p-8">
      <% flash.each do |type, message| %>
        <% tone = type.to_s == "notice" ? "emerald" : "red" %>
        <div class="mb-6 rounded-2xl px-4 py-3 <%= status_bg(tone) %>">
          <%= message %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </div>
</body>
</html>
