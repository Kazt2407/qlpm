<% content_for :title, "Tạo phiếu mượn" %>

<div class="mx-auto max-w-5xl">
  <div class="mb-6">
    <h1 class="text-3xl font-extrabold text-slate-900">Tạo phiếu mượn</h1>
    <p class="mt-2 text-sm text-slate-500">Hỗ trợ teacher, student và system-import theo thời gian bắt đầu/kết thúc.</p>
  </div>
  <div class="shell-card rounded-3xl border border-slate-200/70 p-6">
    <%= render "form", borrow: @borrow %>
  </div>
</div>
