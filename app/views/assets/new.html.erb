<% content_for :title, "Tạo đối tượng" %>

<div class="mx-auto max-w-5xl">
  <div class="mb-6">
    <h1 class="text-3xl font-extrabold text-slate-900">Tạo đối tượng mới</h1>
    <p class="mt-2 text-sm text-slate-500">Dùng chung cho phòng, máy tính, hoặc thiết bị rời.</p>
  </div>
  <div class="shell-card rounded-3xl border border-slate-200/70 p-6">
    <%= render "form", asset: @asset %>
  </div>
</div>
